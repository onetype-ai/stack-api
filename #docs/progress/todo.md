# todo

Ordered by what blocks the most.

## A first plugin

Nothing here has been used. A scaffold that has never carried a capability is
a guess about what carrying one needs.

## Known, and deliberate

- **`src/plugins/` is empty.** The worked examples that used to live here were
  deleted with the packing tool that folded them. What a plugin looks like is
  in `#docs/src/plugin/`.
- **Two tests is the whole suite.** Everything else a project could check
  belongs to the kit and runs there against every project, not only this one.
