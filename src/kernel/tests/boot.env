# The config every test kernel boots with, read as the runtime reads .env:
# <PLUGIN>__<FIELD>=<value>. Development and fake values only, never a secret.
# A plugin with required config adds its lines here and to .env.example.
